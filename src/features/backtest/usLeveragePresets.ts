// QQQ 배수 전략 프리셋 정본 — **UI 무의존 순수 모듈** (presets.ts와 같은 규약).
//
// 2026-08-06 대표 지시로 등재했다. **관문을 통과해서 올라온 것이 아니다** —
// 43차 실측에서 990변형 중 통과 0이었고, 나는 등재에 반대했다. 대표가 그 결과를 보고
// 등재를 확정했으므로 올리되, **탈락 사실과 근거 수치를 라벨·note 맨 앞에 둔다.**
// 수익률만 보고 고르는 것을 막는 것이 이 파일의 목적이다(규칙 4).
//
// ── 왜 사전계산인가 ─────────────────────────────────────────────────────────
//   이 전략의 시세는 tiingo다. 브라우저에서 tiingo를 부르려면 **API 키가 프런트엔드에
//   노출**되므로 규칙 2-1 위반이다. 그래서 GHA에서 구워 JSON으로 커밋하고 화면은 읽기만 한다
//   (`presets-precomputed.json`과 같은 구조). 화면에 "직접 다시 돌리기" 경로가 없는 것은
//   기능 누락이 아니라 **키를 안 내보내려는 설계**다.
//
// ── 이 전략이 무엇인가 ──────────────────────────────────────────────────────
//   평시 QQQ 100% → 고점 대비 낙폭이 깊어지면 비중을 레버리지로 옮기고, 회복하면 되돌린다.
//   엔진은 `leverageLadder.ts`의 `runProportionalLadder`이며 규칙 1(미래참조 금지)을
//   `tests/leverageladder.test.ts`가 절단 불변성으로 강제한다.

import { SPEC_PROPORTIONAL, type ProportionalParams } from './leverageLadder'

/** 사전계산 산출물 스키마 — 화면이 모르는 버전이면 없는 셈 친다(우아한 강등). */
// 스키마 2 (2026-08-06): 프리셋에 `weights`(다운샘플 일별 비중)·`events`(매매 사건)가 추가됐다.
// 스키마 3 (2026-08-07): `dcaHold`(단순 적립 3종 상세 — 반원금 근사·IRR·MDD·칼마)가 추가됐다.
// 스키마 4 (2026-08-07): `bars`(일봉 시가·종가 — 브라우저 재계산용)·`best`(4변수 격자 칼마 1위)가 추가됐다.
// 화면은 하위 버전도 계속 읽는다 — 새 필드가 없으면 해당 표시만 뺀다(없는 값을 지어내지 않는다).
export const US_LEV_SCHEMA = 4
export const US_LEV_SUPPORTED_SCHEMAS: readonly number[] = [1, 2, 3, 4]
export const US_LEV_DATA_URL = 'data/us-leverage-precomputed.json'

export interface UsLeveragePreset {
  id: string
  /** 화면 목록에 뜨는 이름 — **탈락 사실이 먼저 온다** */
  label: string
  /** 한 줄 요약(규칙을 말로) */
  rule: string
  params: ProportionalParams
  /** 경고·한계 전문. 라벨만 보고 고르는 것을 막는다. */
  note: string
}

// ── 공통 경고 ────────────────────────────────────────────────────────────────

/** 모든 변형이 공유하는 탈락 사실. 라벨 뒤 note 맨 앞에 붙는다. */
export const US_LEV_FAILED_PREFIX =
  '❌ **판정 탈락 (2026-08-05 43차).** 이 전략은 우리 관문 3개(①전 구간 알파>0 ②칼마>벤치 ' +
  '③전·후반 둘 다 알파>0) 중 **칼마 관문을 넘지 못했다.** 파라미터 **990변형을 전수 탐색해도 ' +
  '통과 0개**였다. 대표 지시로 기록·비교용으로만 등재한 것이며 **이 프리셋으로 실제 매매를 붙이지 마라.**'

/** 43차 전체가 공유하는 한계. */
export const US_LEV_LIMITS =
  '⚠️ **단순 보유에 졌다.** 같은 구간 QLD 단순보유가 CAGR 33.2%인데 이 계열 최고가 30.0%다. ' +
  '적립식으로 바꿔도 같다 — QLD 단순 적립 9억 1,777만원 vs 사다리 최고 7억 6,747만원(**QLD가 1.20배**). ' +
  '즉 "그냥 QLD를 사서 두는 것"보다 나은 조합을 이 탐색 공간에서 찾지 못했다. ' +
  '⚠️ **낙폭은 QLD만큼 맞으면서 수익만 덜 번다** — 사다리 MDD −62.2% vs QLD −63.3%로 거의 같은데 ' +
  'IRR은 30.7% vs 32.5%다. 낙폭 1.1%p를 줄이려고 수익 1.8%p를 낸 셈이라 교환비가 불리하다. ' +
  '⚠️ **실측 구간(2010-02~)이 이 전략에 구조적으로 유리하다.** TQQQ 상장이 2010-02-11이라 ' +
  '그 이전은 실물 데이터가 존재하지 않고, 2010~2026은 QQQ의 역사적 강세장이며 **모든 하락이 결국 ' +
  '회복됐다.** "떨어지면 배수를 키운다"는 규칙은 그런 구간에서 잘 나올 수밖에 없다. ' +
  '⚠️ **회복되지 않는 하락에서는 계좌가 사라진다.** 합성으로 잰 닷컴 구간(2000-03~2002-10) ' +
  '낙폭은 **−97.8%**, 총수익 −96.7%였다(QQQ 단순보유 −83.0%). 나스닥이 고점 대비 −78% 빠지고 ' +
  '회복까지 15년 걸린 구간에서는 익절 래칫이 작동할 반등 자체가 없다. ' +
  '이 전략의 급소는 파라미터가 아니라 **"모든 하락은 회복된다"는 암묵적 전제** 그 자체다. ' +
  '⚠️ **레버리지 ETF 고유 위험** — 일간 배수를 추종하므로 횡보장에서 변동성 잠식이 복리로 누적되고, ' +
  '3배 상품은 하루 −33.4%에서 이론상 전액 소멸한다. 이 전략은 그 위험을 **낙폭이 가장 깊은 순간에** 떠안는다. ' +
  '⚠️ **환율·세금 미반영.** 원화 투자자 기준 손익이 아니며 미국 배당 원천징수 15%도 빠져 있다 ' +
  '(둘 다 성적을 후하게 만드는 방향). 생존편향은 ETF 3종이라 해당 없음. ' +
  '⚠️ 990변형 중 1등을 고르는 것은 그 자체로 과최적화다 — 아래 수치는 "이 구간에서 가장 운이 좋았던 ' +
  '조합"이지 미래 기댓값이 아니다. **매수 권유가 아니다**(규칙 4).'

// ── 프리셋 ───────────────────────────────────────────────────────────────────

/**
 * **3×3 격자**(2026-08-06 대표 지시): 고배수 갈아타기(진입 낙폭 밴드) 10/15/20% ×
 * 저배수 익절(익절 방아쇠 — 직전 매매가 대비 상승폭) +10/+15/+20%. 총 9종.
 * 2단 진입은 1단의 2배, 익절 규모는 원 지시값 10% 고정.
 *
 * 9종 전부 43차 990변형 전수 탐색 공간(진입밴드 10~30 × 익절방아쇠 10~30 전면격자) 안에
 * 있고 그 탐색에서 **관문 통과는 0개**였다 — 그래서 라벨이 전부 [탈락]으로 시작한다.
 * 개별 조합의 정확한 성적(칼마·CAGR·MDD·알파)은 여기 하드코딩하지 않는다 —
 * **사전계산(MODE=lev:bake)이 매번 다시 계산해 산출물에 싣고 화면이 그 값을 표시한다**(규칙 3).
 * 아래 note의 수치는 43차 당시 실측된 대표 조합의 값으로, 출처를 명시해 남긴다.
 *
 * 격자 1등(칼마 0.58 · 밴드 -22/-32)은 **올리지 않았다** — 전반 8년 알파 +0.0%p,
 * 즉 절반을 아무것도 안 하고 QQQ로 보내서 얻은 값이라 실체가 없기 때문이다.
 */

const BANDS = [10, 15, 20] as const
const TP_STEPS = [10, 15, 20] as const

/** 43차에서 개별 실측이 있는 조합의 노트(밴드×익절+10 열). 나머지는 공통 노트를 쓴다. */
const MEASURED_43: Record<string, string> = {
  'b10-t10':
    ' **43차 실측(GHA MODE=lev:prop · tiingo 총수익 · 2010-02~): 칼마 0.47(벤치 QQQ 0.56) · ' +
    'CAGR +29.5% · MDD −62.7% · 알파 +9.9%p/연 · 전반 +5.9%p · 후반 +14.1%p** · 매매 72건 · ' +
    '평균비중 QQQ 76 / QLD 18 / TQQQ 7. ' +
    '**대표 지시 원값 조합이며 격자에서 수익이 가장 높은 열이다.** 알파 관문과 구간분할 관문은 통과했고 ' +
    '탈락 사유는 칼마 하나다 — 더 벌었지만 **낙폭 대비 효율은 QQQ를 그냥 드는 것보다 나빴다.** ' +
    '⚠️ **MDD −62.7%는 1억이 3,730만원이 되는 구간을 견뎠다는 뜻이다.** ',
  'b15-t10':
    ' **43차 실측: 칼마 0.45 · CAGR +23.6% · MDD −52.8% · 알파 +4.0%p/연 · ' +
    '전반 +3.2%p · 후반 +4.8%p** · 매매 29건. ' +
    '밴드를 10→15%로 넓히면 **낙폭이 10%p 얕아지지만(−62.7%→−52.8%) 알파가 절반 이하로 준다** ' +
    '(+9.9%p→+4.0%p). 칼마는 오히려 0.47→0.45로 **내려간다** — 낙폭이 준 것보다 수익이 더 많이 줄었다. ',
  'b20-t10':
    ' **43차 실측: 칼마 0.51 · CAGR +22.0% · MDD −42.8% · 알파 +2.4%p/연 · ' +
    '전반 +0.0%p · 후반 +4.8%p** · 매매 16건. ' +
    '🔴 **전반 알파가 정확히 +0.0%p다** — 2010~2018에 QQQ가 고점 대비 −20%를 찍은 적이 없어 ' +
    '**사다리가 그 8년 동안 아예 발동하지 않았다.** 낮은 낙폭(−42.8%)은 방어가 아니라 ' +
    '**절반을 그냥 QQQ로 보낸 결과**다. 관문 ③(전·후반 둘 다 알파>0)이 잡으라고 있는 함정이다. ',
}

/** 개별 실측이 없는 조합의 공통 노트 — 지어내지 않고 출처 있는 계열 수치만 쓴다. */
function familyNote(band: number, tp: number): string {
  return (
    ` **이 조합(진입 −${band}%/−${band * 2}% · 익절 +${tp}%)의 개별 수치는 아래 화면 표·차트가 ` +
    '보여주는 사전계산 실측값을 보라** — note에 옮겨 적지 않는 것은 재베이크 때 어긋나기 때문이다(규칙 3). ' +
    '43차 격자 전수 탐색에서 이 조합을 포함한 990변형 전부가 관문을 통과하지 못했고, 계열에서 수익이 가장 ' +
    '높았던 조합조차 **MDD −62.7%**(1억→3,730만원)를 견디고도 칼마가 벤치에 못 미쳤다. ' +
    '익절 방아쇠를 10→30%로 늦추는 축은 43차 스윕에서 칼마를 완만하게 낮추는 방향이었다 — ' +
    '늦게 덜수록 레버리지를 오래 들고 있어 낙폭이 깊어진다. '
  )
}

export const US_LEVERAGE_PRESETS: UsLeveragePreset[] = BANDS.flatMap((band) =>
  TP_STEPS.map((tp) => {
    const key = `b${band}-t${tp}`
    return {
      id: `us-lev-${key}`,
      label: `❌ [탈락] QQQ 배수 사다리 — 진입 ${band}% · 익절 +${tp}% (−${band}%/−${band * 2}% · +${tp}%마다 10% 되돌림)`,
      rule:
        `평시 QQQ 100% → 고점 대비 −${band}%에서 QQQ 절반을 QLD로 → −${band * 2}%에서 남은 QQQ 전부를 TQQQ로 ` +
        `→ 직전 매매가 대비 +${tp}% 오를 때마다 레버리지의 10%를 QQQ로 되돌림 → 신고가 회복 시 QQQ 100%로 초기화`,
      params: { ...SPEC_PROPORTIONAL, band1Pct: band, band2Pct: band * 2, tpStepPct: tp },
      note: US_LEV_FAILED_PREFIX + (MEASURED_43[key] ?? familyNote(band, tp)) + US_LEV_LIMITS,
    }
  }),
)

/** 목록 상단 상시 배너 — 왜 탈락한 것이 올라와 있는지 먼저 말한다. */
export const US_LEV_BANNER =
  '⛔ **이 계열은 판정에서 탈락한 전략입니다.** 43차 실측에서 파라미터 990변형 중 관문 통과가 ' +
  '0개였고, 같은 구간 **QLD를 그냥 사서 두는 것**(CAGR 33.2%)이 이 계열 최고(30.0%)보다 나았습니다. ' +
  '아래 시뮬레이터는 기록·검증용입니다 — 실제 매매에 쓰지 마십시오.'

/** 배너 아래 한 줄 — 무엇을 보라는 안내. */
export const US_LEV_READ_HINT =
  '이 화면은 "무엇을 사라"가 아니라 **"레버리지를 규칙적으로 켜고 끄는 것이 단순 보유를 이기는가"에 ' +
  '대한 실측 답**입니다. 답은 아니오였습니다. 변수를 바꿔 볼 때는 칼마(= CAGR ÷ |MDD|)가 벤치를 ' +
  '넘는지를 먼저 보십시오.'
