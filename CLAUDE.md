# kr-market-leverage-dashboard — 프로젝트 규칙

투자 도메인 코드베이스. 상위 거버넌스는 `ops/governance/GOVERNANCE-INVESTING.md`(투자 T0 포함)를 따른다.
아래는 이 리포에서 **항상 적용되는 절대 규칙**이다.

---

## 🚫 규칙 1 — 미래참조 금지 (Look-Ahead Ban) · 최우선

> **과거 데이터로 시뮬레이션하더라도, 각 시점에서는 그 이후를 모른다는 전제로만 판단한다.
> 미래 값을 가져다 쓰지 마라.**

백테스트가 거짓말을 하는 가장 흔한 경로이자, 이 프로젝트에서 **가장 심각한 결함**으로 취급하는 항목이다.
성능·편의·코드 간결성 어떤 이유로도 완화하지 않는다.

### 반드시 지킬 것

1. **인과성(causality)**: 인덱스 `i` 시점에 계산되는 모든 값은 `bars[0..i]`만 사용한다. `bars[i+1]` 이후를
   읽는 코드는 어떤 형태로도 작성하지 않는다.
2. **신호 → 체결 분리**: 당일 종가로 판단한 신호는 **다음 거래일 시가**에 체결한다(규칙형). 알고리즘형(LOC)은
   당일 종가 체결이 원저 방식이므로 허용하되, 그 종가가 확정된 뒤의 정보는 쓰지 않는다.
   "오늘 종가를 보고 오늘 시가에 샀다"는 계산은 금지.
3. **롤링 극값 시프트**: `HIGHEST`/`LOWEST` 같은 N일 극값은 **당일을 제외한** 직전 N일로 계산한다.
   당일 고가를 포함하면 "오늘 고가를 알고 돌파를 판정"하는 미래참조가 된다.
4. **손절·익절 체결 보수성**: 장중 저가/고가가 기준선에 닿으면 청산하되, 갭으로 관통한 경우 기준가가 아니라
   **시가(더 불리한 쪽)**로 체결한다. 유리한 쪽으로 가정하지 않는다.
5. **전체 구간 통계 금지**: 정규화·스케일링·임계값 산출에 전체 기간의 평균·표준편차·최대최소를 쓰지 않는다
   (그 자체가 미래 정보다). 필요하면 시점까지의 확장 윈도우로만 계산한다.
6. **마지막 봉 신규 진입 금지**: 데이터 마지막 봉에서는 체결할 다음 봉이 없으므로 신규 진입을 만들지 않는다.
7. **생존편향 인지**: 상장폐지·합병 종목이 빠진 유니버스는 성적을 부풀린다. 결과 해석 시 반드시 병기한다.

### 강제 수단 (문서로 끝내지 않는다)

- `tests/lookahead.test.ts`의 **절단 불변성(truncation invariance)** 테스트가 이 규칙의 집행자다.
  같은 데이터의 뒷부분을 잘라내고 다시 돌렸을 때, 잘린 시점 이전의 매매·자산곡선이 **완전히 동일**해야 한다.
  하나라도 달라지면 어딘가에서 미래를 봤다는 뜻이므로 테스트가 실패한다.
- 엔진(`engine.ts`, `algoEngine.ts`, `series.ts`)이나 지표를 건드리면 **반드시** `npm test`를 돌리고
  통과를 확인한 뒤 커밋한다. 실패한 채로 커밋·배포하지 않는다.
- 새 지표·새 전략을 추가하면 그 경로를 덮는 절단 불변성 케이스를 테스트에 **함께** 추가한다.

---

## 규칙 2 — 실계좌 경계 (투자 T0 · 단계적 해제)

> **2026-07-29 대표 채택.** 근거·게이트 상세: `ops/context/investing/규칙2-개정안_2026-07-28.md`
> 구 규칙은 브로커 API 연동을 준비 단계까지 전면 금지했는데, 분봉을 주는 무료·합법 경로가
> 브로커 API뿐이라 **장중 전략은 검증 자체가 불가능** → "검증 없이 실계좌 직행"이라는
> 더 나쁜 결과를 낳는 구조였다. 그래서 **조회와 주문을 분리**한다.

자금 이체·입출금 API는 **영구 금지**한다. 브로커 API는 아래 3단계로만 열린다.
각 단계는 대표의 **명시적·개별 승인**으로만 열리며, 앞 단계 게이트를 통과하지 못하면
다음 단계는 열리지 않는다. **승인 없이 다음 단계 코드를 미리 만들어두지 않는다.**

- **1단계 조회 전용** — 분봉·시세·조건검색 수신만. 주문 엔드포인트 문자열이 코드에 존재해서는
  안 되며 `tests/no-order-endpoint.test.ts`가 이를 강제한다. 전용 계좌·잔고 0 유지.
  통과 조건: 장중 신호 기준 백테스트로 알파 산출. **알파가 음수면 여기서 종료**하고 2단계로 안 간다.
- **2단계 모의투자 주문** — 증권사 모의 서버 한정. 실서버 주소를 코드에 두지 않는다.
  주문 함수 `dryRun` 기본 `true`. 1회 주문액·일일 건수 상한을 코드 하드 게이트로.
  통과 조건: 최소 4주 연속 모의 운용 + 슬리피지·미체결률 실측 대조.
- **3단계 실계좌 소액** — 총 투입 한도·일일 손실 한도·1회 주문 상한·최대 보유 종목수·
  킬 스위치(`HALT` 파일)를 전부 코드 게이트로 강제. **기동은 대표가 수동으로** 한다.

계좌 자격증명은 Doppler `investing-ops`에 두고 **AI 세션에 부여하지 않는다.**

**개정 후에도 안 바뀌는 것**: 자금 이체 영구 금지 / AI 세션에 API키·DOPPLER_TOKEN 미부여
(값 입력은 대표만 T0) / 미래참조 금지(규칙 1) 전면 유지 — 분봉이 들어와도 절단 불변성 테스트
통과 필수 / 투자자문 아님(규칙 4) / **AI는 실계좌 주문을 직접 실행하지 않는다** — 코드를 만들 뿐
기동은 대표.

**현재 열린 단계: 없음.** 대표가 "1단계 열어"라고 명시하기 전까지 브로커 API 연동에 착수하지 않는다.

## 규칙 2-1 — 시크릿은 Doppler가 단일 원본

키·토큰·자격증명을 다루는 작업이면 **착수 전에** `ops/governance/SECRETS-POLICY.md`를 읽는다.
요지: 시크릿 단일 원본 = **Doppler**(투자 도메인 프로젝트 `investing-ops` / config `prd`).
신규 시크릿을 평문 파일·플랫폼 env에 직접 넣지 않는다. 값 입력·토큰 발급은 대표만(T0).

- 로딩 코드를 새로 짜지 말고 **`scripts/lib/loadSecret.mjs` 하나만** 쓴다.
  Doppler 우선 → env → 파일(레거시) 순서, 출처를 `✅ Doppler` / `⚠️ 폴백`으로 로그에 남기고
  값은 어떤 경로로도 출력하지 않는다(길이만). `tests/loadsecret.test.ts`가 이를 강제한다.
- 실행 표준: `doppler run --project investing-ops --config prd -- node scripts/<script>.mjs`
- **하드코딩 기본 경로 금지** — `DEFAULT_KEY_FILE = 'C:/…/secrets/X.txt'` 같은 상수는 두지 않는다.
  다음 세션이 그대로 복사한다(실제로 그렇게 사고가 났다). 경로는 `<NAME>_FILE` 환경변수로만 받는다.
- 리포에 남아 있는 평문 키 파일 관행은 **이관 대상이지 따라 할 예시가 아니다.**

## 규칙 3 — 데이터 정직성

- 실데이터와 샘플·추정치를 배지·라벨로 반드시 구분한다. 샘플을 실데이터인 양 제시하지 않는다.
- 가격 시계열은 **분할·배당 보정 상태를 명시**한다. Yahoo v8의 `indicators.quote` OHLC는 분할만 반영하고
  배당은 `adjclose`에만 반영되므로, `adjclose ÷ close` 계수를 OHLC에 곱해 총수익 기준으로 변환한다.
- 데이터 출처·지연·한계(비공식 엔드포인트·정확성 미보증·생존편향·환율 미반영)를 화면에서 확인 가능하게 둔다.
- "완료"는 빌드·테스트 통과 + 라이브 URL 실동작 확인까지. 미확인 항목은 `[미검증]`으로 표기한다.

## 규칙 4 — 투자자문 아님 · 리스크 병기

확정적 매수/매도 권유 표현을 쓰지 않는다(관찰·조건·확률 프레임). 수익 관련 산출물에는 손실 경로·최대낙폭·
무효화 지점을 같은 무게로 병기하고, 레버리지 ETF는 변동성 잠식·극단 낙폭 경고를 유지한다. 모든 투자
산출물 하단에 표준 고지문을 남긴다.

## 규칙 5 — 성과 판정은 알파로

모델 평가는 절대 수익률이 아니라 **알파(연환산 수익률 − 벤치마크 연환산 수익률)** 기준으로 한다.
장세가 좋아 번 것을 실력으로 오인하지 않기 위함이다. 단일 구간 성적은 근거로 부족하며, 구간 분할
검증(홀드아웃·워크포워드)의 일관성을 함께 본다.

---

## 개발

```bash
npm install
npm run dev     # 로컬 개발 서버
npm test        # 엔진 불변식 검증 (미래참조 금지 포함) — 엔진 수정 시 필수
npm run build   # 타입체크 + 프로덕션 빌드
```

배포: `main` 빌드 산출물(`dist/`)을 `gh-pages` 브랜치에 올려 GitHub Pages로 서빙한다.
