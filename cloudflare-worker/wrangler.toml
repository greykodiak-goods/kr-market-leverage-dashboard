# Optional: deploy the CORS proxy with the Wrangler CLI.
# (The dashboard method in README.md does not need this file.)
name = "kr-market-proxy"
main = "proxy.js"
compatibility_date = "2024-11-01"

# After `npx wrangler deploy`, the worker is served at:
#   https://kr-market-proxy.<your-subdomain>.workers.dev
